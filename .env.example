# Required — LLM API keys
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=AIza...

# Optional — semantic scoring (falls back to llm_judge if absent)
VOYAGE_API_KEY=pa-...

# App config
ALLOWED_ORIGIN=https://evalkit.danblanco.dev
DEMO_MODE=false

# Port exposed by Docker Compose (default: 80)
PORT=80
